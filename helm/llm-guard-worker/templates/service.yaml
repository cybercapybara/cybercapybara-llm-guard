apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-guard-worker.fullname" . }}
  labels:
    {{- include "llm-guard-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
      name: health
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "llm-guard-worker.selectorLabels" . | nindent 4 }}
