{{- if and .Values.pdb.enabled (gt (int .Values.replicaCount) 1) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "llm-guard-worker.fullname" . }}
  labels:
    {{- include "llm-guard-worker.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "llm-guard-worker.selectorLabels" . | nindent 6 }}
{{- end }}
